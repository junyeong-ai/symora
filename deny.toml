[graph]
all-features = true

[output]
feature-depth = 1

[advisories]
yanked = "deny"
# Transitively-unmaintained crates (e.g. `paste` deep inside fastembed's
# image/tokenizers tree) are upstream's call, not ours — only flag direct
# workspace dependencies.
unmaintained = "workspace"

[licenses]
confidence-threshold = 0.95
allow = [
  "MIT",
  "MIT-0",
  "Apache-2.0",
  "Apache-2.0 WITH LLVM-exception",
  "BSD-2-Clause",
  "BSD-3-Clause",
  "BSL-1.0",
  "CC0-1.0",
  "CDLA-Permissive-2.0",
  "ISC",
  "MPL-2.0",
  "Unicode-3.0",
  "Unlicense",
  "Zlib",
]
exceptions = [
  # fastembed → image → ravif → rav1e (fuzz harness); "(MIT OR Apache-2.0) AND NCSA".
  { allow = ["NCSA"], crate = "libfuzzer-sys" },
]

[bans]
multiple-versions = "warn"
wildcards = "deny"
allow-wildcard-paths = false

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
