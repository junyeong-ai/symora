[project]
name = "fixture-monorepo"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["fixture-core", "fixture-app"]

[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
fixture-core = { workspace = true }
fixture-app = { workspace = true }
